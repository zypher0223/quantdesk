[pytest]
# `pytest` from the repository root must work: the engine's own configuration lives
# in engine/pyproject.toml, but a run started above it used to fail at collection
# because `from tests.…` imports could not resolve. These three lines make the root
# an equally valid place to start.
testpaths = engine/tests
pythonpath = engine engine/src
asyncio_mode = auto
